<view wx:if="{{location}}" class="container">
  <map class="container" latitude='{{location.latitude}}' longitude='{{location.longitude}}' scale='{{scale}}' markers='{{markers}}' show-location></map>
</view>